import { THEME, Theme } from "data/constants";

export const DEV_MODE = true; // Change to false for production mode

export const DEFAULT_THEME: Theme = THEME.Light;
export const SCROLL_POS_THRESHOLD = 1000;
export const ROUNDING_PRECISION = 5;
export const MIN_DISPLAYING_DECIMAL_PLACE = 0;
export const MAX_DISPLAYING_DECIMAL_PLACE = 2;
